import re
import os


def Seeker():
    files = os.listdir()
    nofold = []
    for file in files:
        if os.path.isfile(file):
            nofold.append(file)
    return nofold


def 
text = []
text = Seeker()
print (text)
